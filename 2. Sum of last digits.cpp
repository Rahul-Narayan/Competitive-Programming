#include <bits/stdc++.h>
using namespace std;

// int main() {
//   string n, m;
//   cin >> n >> m;
//   cout << (n[n.size()-1]-'0') + (m[m.size()-1]-'0');

// return 0;
// }

int main()
{
  long long a, b;
  cin >> a >> b;
  cout << (a % 10) + (b % 10);

  return 0;
}
